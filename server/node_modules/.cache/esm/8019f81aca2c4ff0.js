let mongoose;_ca2‍.x([["default",()=>_ca2‍.o]]);_ca2‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);
const { ObjectId } = mongoose.Schema;

const orderSchema = new mongoose.Schema(
  {
    hotel: {
      type: ObjectId,
      ref: "Hotel",
    },
    session: {},
    orderedBy: { type: ObjectId, ref: "User" },
  },
  { timestamps: true }
);

_ca2‍.d(mongoose.model("Order",orderSchema));
